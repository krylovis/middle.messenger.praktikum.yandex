<input class="input {{#if modifier}}input_type_{{modifier}}{{/if}}" id="{{ id }}" type="{{ type }}" name="{{ name }}"
  {{#if value}}value="{{value}}" {{/if}} placeholder="{{ placeholder }}" minlength="2" maxlength="30" required {{#if
  disabled}}disabled{{/if}}>